fn main() {
    let v1 = vec![1, 2, 3];

    let iter = v1.iter();


    for value in iter {
        println!("Got: {:?}", value);
    }
    iter.nex
    let a =  iter;
}

fn consume_it(value:i32) {}

