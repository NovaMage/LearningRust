pub fn get_groups_where_domain_is_personal() -> Vec<u32> {
    println!("Getting groups where domain is personal...");
    vec![1, 2, 3]
}
pub fn get_groups_where_domain_is_main() -> Vec<u32> {
    println!("Getting groups where domain is main...");
    vec![1, 2, 3]
}
